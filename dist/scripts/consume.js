var grephy;
(function (grephy) {
    var Consume = /** @class */ (function () {
        function Consume() {
        }
        Consume.init = function () {
            // clear everything & all variables 
        };
        return Consume;
    }());
    grephy.Consume = Consume;
})(grephy || (grephy = {}));
