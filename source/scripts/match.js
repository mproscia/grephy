var grephy;
(function (grephy) {
    var Match = /** @class */ (function () {
        function Match() {
        }
        Match.init = function () {
            // reset matching variables
            input = [];
        };
        Match.matchTokens = function () {
        };
        return Match;
    }());
    grephy.Match = Match;
})(grephy || (grephy = {}));
