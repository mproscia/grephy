var grephy;
(function (grephy) {
    var Match = /** @class */ (function () {
        function Match() {
        }
        Match.init = function () {
            // reset matching variables
        };
        return Match;
    }());
    grephy.Match = Match;
})(grephy || (grephy = {}));
